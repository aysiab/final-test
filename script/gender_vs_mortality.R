# Character Gender vs Mortality Rate 

library(ggplot2)
library(dplyr)

GenderMortality <- function(dataset) {
  all.data <- dataset
  }
  

GvMtable <- function(dataset) {
  living <- dataset %>%
    filter()
}
  # select gender 
  # select mortality 
  