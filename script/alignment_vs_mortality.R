# Character Alignment vs Mortality Rate 

library(ggplot2)
library(dplyr)

AlignmentMortality <- function(dataset) {
  all.data <- dataset
  }
  

AvMtable <- function(dataset) {
  living <- dataset %>%
    filter()
}
  # select gender
  # select alignment 
  # select mortality 
  
}